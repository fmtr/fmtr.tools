[project]
name = "corio"
version = "2.9.0"
description = "Collection of high-level tools to simplify everyday development tasks, with a focus on AI/ML"
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = []
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = []

[project.optional-dependencies]
"path.search" = ["ripgrep-rs"]
compute = ["torch>=2.12", "torchvision", "torchaudio"]
dev = ["logfire", "semver", "pydevd-pycharm~=261.26222.68", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml", "beanie", "setuptools", "mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav", "build", "twine", "packaging", "pygit2", "python-on-whales", "deepmerge", "httpx", "httpx_retries", "logfire[httpx]", "fastapi", "uvicorn[standard]", "logfire[fastapi]", "tomlkit", "pyrage", "dotenv", "pytest", "pytest-cov", "pytest-asyncio", "qdrant-client[fastembed]", "flagembedding", "torch>=2.12", "torchvision", "torchaudio", "pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx"]
test = ["pytest", "pytest-cov", "pytest-asyncio"]
yml = ["yamlscript>=0.2.20", "pyyaml"]
yaml = ["yamlscript>=0.2.20", "pyyaml"]
logging = ["logfire"]
parallel = ["dask[bag]", "distributed", "bokeh"]
tokenization = ["tokenizers"]
augmentation = ["faker", "sre_yield"]
process = ["logfire"]
profiling = ["contexttimer"]
"docker.client" = ["python-on-whales"]
unicode = ["Unidecode"]
version = []
"version.dev" = ["semver"]
spaces = ["tinynetrc"]
netrc = ["tinynetrc"]
hfh = ["huggingface_hub"]
merging = ["deepmerge"]
api = ["fastapi", "uvicorn[standard]", "logfire", "pydantic", "pydantic-extra-types", "pycountry", "logfire[fastapi]", "httpx"]
"api.mcp" = ["fastapi", "uvicorn[standard]", "logfire", "pydantic", "pydantic-extra-types", "pycountry", "logfire[fastapi]", "httpx", "fastmcp"]
dm = ["pydantic", "pydantic-extra-types", "pycountry"]
ai = ["peft", "transformers[sentencepiece]", "torchvision", "torchaudio", "pydantic", "pydantic-extra-types", "pycountry"]
"openai.api" = ["openai"]
"ai.client" = ["logfire", "pydantic", "pydantic-extra-types", "pycountry", "openai", "pydantic-ai-slim[logfire,openai]", "ollama"]
"agentic.acp" = ["agent-client-protocol"]
json-fix = ["json_repair"]
semantic = ["sentence_transformers", "pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx"]
metrics = ["pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx"]
tabular = ["pandas", "tabulate", "openpyxl", "odfpy", "deepdiff"]
html = ["html2text"]
interface = ["flet[all] <0.80.0", "flet-video", "flet-webview"]
"google.api" = ["google-auth", "google-auth-oauthlib", "google-auth-httplib2", "google-api-python-client"]
caching = ["diskcache", "cachetools"]
pdf = ["pymupdf", "pydantic", "pydantic-extra-types", "pycountry", "pymupdf4llm"]
debug = ["pydevd-pycharm~=261.26222.68"]
sets = ["pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml"]
"path.app" = ["appdirs"]
"path.type" = ["filetype"]
dns = ["dnspython[doh]", "httpx", "httpx_retries", "logfire", "logfire[httpx]", "diskcache", "cachetools"]
dhcp = ["httpx", "httpx_retries", "logfire", "logfire[httpx]"]
patterns = ["regex"]
http = ["httpx", "httpx_retries", "logfire", "logfire[httpx]"]
setup = ["setuptools"]
webhook = ["httpx", "httpx_retries", "logfire", "logfire[httpx]"]
browsers = ["playwright==1.61.0"]
db = []
"db.document" = ["beanie"]
"db.search" = ["qdrant-client[fastembed]", "pydantic", "pydantic-extra-types", "pycountry", "flagembedding", "torch>=2.12", "torchvision", "torchaudio", "pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx"]
mqtt = ["aiomqtt"]
av = ["av"]
env = []
"env.io" = ["dotenv"]
toml = []
"toml.write" = ["tomlkit"]
ha = ["dotenv"]
"ha.api" = ["dotenv", "homeassistant_api", "aiohasupervisor"]
doc = ["mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav"]
youtube = ["pytubefix"]
infra = ["semver", "logfire", "setuptools", "mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml", "build", "twine", "packaging", "pygit2", "python-on-whales", "deepmerge", "httpx", "httpx_retries", "logfire[httpx]", "fastapi", "uvicorn[standard]", "logfire[fastapi]", "tomlkit", "pyrage", "dotenv", "pytest", "pytest-cov", "pytest-asyncio"]
vcs = ["pygit2"]
tasmota = ["decode-config", "esptool"]
encrypt = ["pyrage"]
secrets = ["pyrage", "dotenv", "yamlscript>=0.2.20", "pyyaml", "logfire", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "pygit2"]
sec = ["pyrage", "dotenv", "yamlscript>=0.2.20", "pyyaml", "logfire", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "pygit2"]
cli = ["pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml", "logfire"]
all = ["ripgrep-rs", "torch>=2.12", "torchvision", "torchaudio", "logfire", "semver", "pydevd-pycharm~=261.26222.68", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml", "beanie", "setuptools", "mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav", "build", "twine", "packaging", "pygit2", "python-on-whales", "deepmerge", "httpx", "httpx_retries", "logfire[httpx]", "fastapi", "uvicorn[standard]", "logfire[fastapi]", "tomlkit", "pyrage", "dotenv", "pytest", "pytest-cov", "pytest-asyncio", "qdrant-client[fastembed]", "flagembedding", "pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx", "dask[bag]", "distributed", "bokeh", "tokenizers", "faker", "sre_yield", "contexttimer", "Unidecode", "tinynetrc", "huggingface_hub", "fastmcp", "peft", "transformers[sentencepiece]", "openai", "pydantic-ai-slim[logfire,openai]", "ollama", "agent-client-protocol", "json_repair", "sentence_transformers", "html2text", "flet[all] <0.80.0", "flet-video", "flet-webview", "google-auth", "google-auth-oauthlib", "google-auth-httplib2", "google-api-python-client", "diskcache", "cachetools", "pymupdf", "pymupdf4llm", "appdirs", "filetype", "dnspython[doh]", "regex", "playwright==1.61.0", "aiomqtt", "av", "homeassistant_api", "aiohasupervisor", "pytubefix", "decode-config", "esptool"]

[[project.authors]]
name = "Frontmatter AI"
email = "innovative.fowler@mask.pro.fmtr.dev"

[project.urls]
Homepage = "https://github.com/fmtr/corio"

[project.scripts]
corio = "corio.entrypoint:main"

[tool.corio.metadata]
version = "2.9.0"
port = 0
base = "python"
description = "Collection of high-level tools to simplify everyday development tasks, with a focus on AI/ML"
org_singleton = "fmtr"
org_github = "fmtr"
org_friendly = "Frontmatter AI"
is_client = false
scripts = []
services = []
keywords = []
is_pypi = true
is_dockerhub = false
test_envs = true

[tool.corio.metadata.setup]

[tool.corio.dependencies]
"path.search" = ["ripgrep-rs"]
compute = ["torch>=2.12", "torchvision", "torchaudio"]
dev = ["logging", "version.dev", "debug", "sets", "yaml", "db.document", "infra", "db.search"]
install = []
test = ["pytest", "pytest-cov", "pytest-asyncio"]
yml = ["yamlscript>=0.2.20", "pyyaml"]
yaml = ["yml"]
logging = ["logfire", "version"]
parallel = ["dask[bag]", "distributed", "bokeh"]
tokenization = ["tokenizers"]
augmentation = ["faker", "sre_yield"]
process = ["logging"]
profiling = ["contexttimer"]
"docker.client" = ["python-on-whales"]
unicode = ["Unidecode"]
version = []
"version.dev" = ["semver"]
spaces = ["netrc"]
netrc = ["tinynetrc"]
hfh = ["huggingface_hub"]
merging = ["deepmerge"]
api = ["fastapi", "uvicorn[standard]", "logging", "dm", "logfire[fastapi]", "httpx"]
"api.mcp" = ["api", "fastmcp"]
dm = ["pydantic", "pydantic-extra-types", "pycountry"]
ai = ["peft", "transformers[sentencepiece]", "torchvision", "torchaudio", "dm"]
"openai.api" = ["openai"]
"ai.client" = ["logging", "dm", "openai.api", "pydantic-ai-slim[logfire,openai]", "ollama"]
"agentic.acp" = ["agent-client-protocol"]
json-fix = ["json_repair"]
semantic = ["sentence_transformers", "metrics"]
metrics = ["tabular", "ranx"]
tabular = ["pandas", "tabulate", "openpyxl", "odfpy", "deepdiff"]
html = ["html2text"]
interface = ["flet[all] <0.80.0", "flet-video", "flet-webview"]
"google.api" = ["google-auth", "google-auth-oauthlib", "google-auth-httplib2", "google-api-python-client"]
caching = ["diskcache", "cachetools"]
pdf = ["pymupdf", "dm", "pymupdf4llm"]
debug = ["pydevd-pycharm~=261.26222.68"]
sets = ["pydantic-settings", "dm", "yaml"]
"path.app" = ["appdirs"]
"path.type" = ["filetype"]
dns = ["dnspython[doh]", "http", "caching"]
dhcp = ["http"]
patterns = ["regex"]
http = ["httpx", "httpx_retries", "logging", "logfire[httpx]"]
setup = ["setuptools"]
webhook = ["http"]
browsers = ["playwright==1.61.0"]
db = []
"db.document" = ["beanie"]
"db.search" = ["qdrant-client[fastembed]", "dm", "flagembedding", "compute", "metrics"]
mqtt = ["aiomqtt"]
av = ["av"]
env = []
"env.io" = ["dotenv"]
toml = []
"toml.write" = ["tomlkit"]
ha = ["env.io"]
"ha.api" = ["ha", "homeassistant_api", "aiohasupervisor"]
doc = ["mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav"]
youtube = ["pytubefix"]
infra = ["version.dev", "logging", "setup", "doc", "sets", "build", "twine", "packaging", "vcs", "docker.client", "merging", "http", "api", "toml.write", "secrets", "cli", "test"]
vcs = ["pygit2"]
tasmota = ["decode-config", "esptool"]
encrypt = ["pyrage"]
secrets = ["encrypt", "env.io", "yaml", "logging", "sets", "vcs"]
sec = ["secrets"]
cli = ["sets", "logging"]

[tool.setuptools]
script-files = ["scripts/add-service", "scripts/add-user-path", "scripts/add-ve-shell", "scripts/apt-essentials", "scripts/apt-headless", "scripts/auth-token", "scripts/compose-update", "scripts/compress-dir", "scripts/cru", "scripts/docker-build-bases", "scripts/docker-build-bases-dev", "scripts/docker-install-deps", "scripts/docker-install-prod", "scripts/docker-prune", "scripts/docker-sandbox", "scripts/docker-sandbox-init", "scripts/docs-deploy", "scripts/download", "scripts/egpu-eject", "scripts/encrypt-secrets", "scripts/fmtr-test-script", "scripts/git-clone", "scripts/ha-addon-launch", "scripts/install-browser", "scripts/install-docker", "scripts/install-docker-cuda", "scripts/install-ts", "scripts/mirror-dir", "scripts/opt-dev-init", "scripts/parse-args", "scripts/pypi-check", "scripts/pypi-reserve", "scripts/run-script", "scripts/run-tests", "scripts/set-password", "scripts/set-secure-path", "scripts/set-user-sudo", "scripts/snips-install", "scripts/ssh-auth", "scripts/ssh-serve", "scripts/tasmota-config", "scripts/tasmota-flash", "scripts/tasmota-terminal", "scripts/infra", "scripts/vm-launch"]

[tool.setuptools.packages.find]
where = ["."]
include = ["corio*"]
namespaces = false

[tool.setuptools.package-data]
corio = ["pyproject.package.toml", "assets/**"]

[tool.tox]
requires = ["tox>=4.22", "tox-uv>=1"]
env_list = ["api", "caching", "datatype", "dhcp", "dns", "dt", "encrypt", "env", "hash", "hook", "infra", "inherit", "iterator", "jsn", "name", "path", "patterns", "rand", "sec", "sets", "shell", "strings", "tasmota", "toml", "tools", "yml"]

[tool.tox.env.api]
description = "Run api tests."
deps = ["fastapi", "fastmcp", "httpx", "logfire", "logfire[fastapi]", "pycountry", "pydantic", "pydantic-extra-types", "pytest", "pytest-asyncio", "pytest-cov", "uvicorn[standard]"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_api.py"]]

[tool.tox.env.caching]
description = "Run caching tests."
deps = ["cachetools", "diskcache", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_caching.py"]]

[tool.tox.env.datatype]
description = "Run datatype tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_datatype.py"]]

[tool.tox.env.dhcp]
description = "Run dhcp tests."
deps = ["httpx", "httpx_retries", "logfire", "logfire[httpx]", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_dhcp.py"]]

[tool.tox.env.dns]
description = "Run dns tests."
deps = ["cachetools", "diskcache", "dnspython[doh]", "httpx", "httpx_retries", "logfire", "logfire[httpx]", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_dns.py"]]

[tool.tox.env.dt]
description = "Run dt tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_dt.py"]]

[tool.tox.env.encrypt]
description = "Run encrypt tests."
deps = ["pyrage", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_encrypt.py"]]

[tool.tox.env.env]
description = "Run env tests."
deps = ["dotenv", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_env.py"]]

[tool.tox.env.hash]
description = "Run hash tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_hash.py"]]

[tool.tox.env.hook]
description = "Run hook tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_hook.py"]]

[tool.tox.env.infra]
description = "Run infra tests."
deps = ["build", "deepmerge", "dotenv", "fastapi", "httpx", "httpx_retries", "logfire", "logfire[fastapi]", "logfire[httpx]", "mike", "mkdocs", "mkdocs-include-dir-to-nav", "mkdocs-material", "mkdocstrings[python]", "packaging", "pycountry", "pydantic", "pydantic-extra-types", "pydantic-settings", "pygit2", "pyrage", "pytest", "pytest-asyncio", "pytest-cov", "python-on-whales", "pyyaml", "semver", "setuptools", "tomlkit", "twine", "uvicorn[standard]", "yamlscript>=0.2.20"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_infra.py"]]

[tool.tox.env.inherit]
description = "Run inherit tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_inherit.py"]]

[tool.tox.env.iterator]
description = "Run iterator tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_iterator.py"]]

[tool.tox.env.jsn]
description = "Run jsn tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_jsn.py"]]

[tool.tox.env.name]
description = "Run name tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_name.py"]]

[tool.tox.env.path]
description = "Run path tests."
deps = ["appdirs", "filetype", "pytest", "pytest-asyncio", "pytest-cov", "ripgrep-rs"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_path.py"]]

[tool.tox.env.patterns]
description = "Run patterns tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov", "regex"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_patterns.py"]]

[tool.tox.env.rand]
description = "Run rand tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_rand.py"]]

[tool.tox.env.sec]
description = "Run sec tests."
deps = ["dotenv", "logfire", "pycountry", "pydantic", "pydantic-extra-types", "pydantic-settings", "pygit2", "pyrage", "pytest", "pytest-asyncio", "pytest-cov", "pyyaml", "yamlscript>=0.2.20"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_sec.py"]]

[tool.tox.env.sets]
description = "Run sets tests."
deps = ["pycountry", "pydantic", "pydantic-extra-types", "pydantic-settings", "pytest", "pytest-asyncio", "pytest-cov", "pyyaml", "yamlscript>=0.2.20"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_sets.py"]]

[tool.tox.env.shell]
description = "Run shell tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_shell.py"]]

[tool.tox.env.strings]
description = "Run strings tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_strings.py"]]

[tool.tox.env.tasmota]
description = "Run tasmota tests."
deps = ["decode-config", "esptool", "pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_tasmota.py"]]

[tool.tox.env.toml]
description = "Run toml tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov", "tomlkit"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_toml.py"]]

[tool.tox.env.tools]
description = "Run tools tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_tools.py"]]

[tool.tox.env.yml]
description = "Run yml tests."
deps = ["pytest", "pytest-asyncio", "pytest-cov", "pyyaml", "yamlscript>=0.2.20"]
commands = [["python", "-m", "pytest", "-q", "corio/tests/test_yml.py"]]

[build-system]
requires = ["setuptools>=82", "wheel"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = ["logfire", "semver", "pydevd-pycharm~=261.26222.68", "pydantic-settings", "pydantic", "pydantic-extra-types", "pycountry", "yamlscript>=0.2.20", "pyyaml", "beanie", "setuptools", "mkdocs", "mkdocs-material", "mkdocstrings[python]", "mike", "mkdocs-include-dir-to-nav", "build", "twine", "packaging", "pygit2", "python-on-whales", "deepmerge", "httpx", "httpx_retries", "logfire[httpx]", "fastapi", "uvicorn[standard]", "logfire[fastapi]", "tomlkit", "pyrage", "dotenv", "pytest", "pytest-cov", "pytest-asyncio", "qdrant-client[fastembed]", "flagembedding", "torch>=2.12", "torchvision", "torchaudio", "pandas", "tabulate", "openpyxl", "odfpy", "deepdiff", "ranx"]
