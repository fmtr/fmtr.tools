{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"<p>Start Here</p> <p>Use Quick Start, then the Modules Overview.</p>"},{"location":"#corio","title":"Corio","text":"<p>Corio is a practical Python toolkit for building small services, automation, data pipelines, and AI/ML support code without re-solving the same project plumbing every time.</p> <p>It bundles the pieces that tend to sit between \"standard library\" and \"application code\": typed paths with serialization helpers, pydantic-friendly data models, CLI settings, structured logging spans, iterator progress, FastAPI/MCP endpoint scaffolding, release metadata generation, test environment generation, and opt-in integrations for docs, Docker, search, tabular data, PDFs, OpenAI, Home Assistant, DNS, caching, encryption, and more.</p> <p>Corio is organized as one package with optional extras. Install the small core by default, then opt into the heavier tools a project actually needs.</p>"},{"location":"#install","title":"Install","text":"<pre><code>pip install corio\n</code></pre> <p>Examples:</p> <pre><code>pip install \"corio[api]\"\npip install \"corio[tabular]\"\npip install \"corio[db.search]\"\npip install \"corio[infra]\"\n</code></pre>"},{"location":"#why-use-it","title":"Why Use It","text":"<ul> <li>Use paths as real application objects. Read and write JSON, YAML, TOML, text, and derived paths directly from <code>corio.Path</code>.</li> <li>Make CLIs from typed models. Corio builds on pydantic settings patterns so small tools can stay declarative.</li> <li>Wrap iteration with observability. <code>corio.iterator.Iterator</code> tracks count, percentage, elapsed time, ETA, completion, and cleanup.</li> <li>Expose tools as HTTP and MCP endpoints. Define endpoint classes once and mount them into FastAPI and MCP surfaces.</li> <li>Keep release metadata generated. <code>corio pyproject</code> turns Corio dependency groups and project metadata into the generated <code>pyproject.toml</code> sections.</li> <li>Install integrations only when needed. Extras keep heavy dependencies like API servers, vector search, ML, PDFs, and tabular tooling out of the base install.</li> </ul>"},{"location":"#examples","title":"Examples","text":""},{"location":"#typed-paths-and-data-files","title":"Typed Paths And Data Files","text":"<pre><code>from corio import Path\n\npath = Path(\"runs/latest/config.yaml\")\npath.parent.mkdirf()\npath.write_data({\n    \"model\": \"embedder-v2\",\n    \"batch_size\": 64,\n})\n\nconfig = path.read_data()\njson_path = path.get_conversion_path(\"json\")\njson_path.parent.mkdirf()\njson_path.write_data(config)\n</code></pre>"},{"location":"#observable-iteration","title":"Observable Iteration","text":"<pre><code>from corio.iterator import Iterator\n\ndocuments = [{\"id\": \"a\"}, {\"id\": \"b\"}, {\"id\": \"c\"}]\n\nfor document in Iterator(documents):\n    # Work happens inside per-item logging spans with progress stats.\n    process(document)\n</code></pre> <p>The iterator tracks <code>count</code>, <code>total</code>, <code>percentage</code>, elapsed time, rate, ETA, completion logs, and closes the wrapped iterator when possible.</p>"},{"location":"#fastapi-and-mcp-tools","title":"FastAPI And MCP Tools","text":"<pre><code>from functools import cached_property\n\nfrom corio import api\n\n\nclass Search(api.endpoint.API):\n    \"\"\"Search indexed documents.\"\"\"\n\n    PATH = \"/search\"\n    TAGS = \"documents\"\n\n    async def run(self, query: str):\n        return {\"query\": query, \"results\": []}\n\n\nclass Reindex(api.endpoint.Tool):\n    \"\"\"Rebuild the document index.\"\"\"\n\n    async def run(self):\n        return {\"status\": \"queued\"}\n\n\nclass Service(api.Base):\n    TITLE = \"Document Service\"\n    PORT = 8080\n\n    @cached_property\n    def ENDPOINTS(self):\n        return [Search, Reindex]\n\n\nService().launch()\n</code></pre> <p>The same endpoint model can feed HTTP routes and MCP tools, so internal automation and external APIs stay aligned.</p>"},{"location":"#project-metadata","title":"Project Metadata","text":"<p>Corio projects keep dependency intent in <code>[tool.corio.dependencies]</code>; generated package metadata lives in <code>[project.optional-dependencies]</code>.</p> <pre><code>[tool.corio.dependencies]\nsemantic = [\"sentence_transformers\", \"metrics\"]\nmetrics = [\"tabular\", \"ranx\"]\ntabular = [\"pandas\", \"tabulate\", \"openpyxl\", \"odfpy\", \"deepdiff\"]\n</code></pre> <p>Then regenerate:</p> <pre><code>corio pyproject\n</code></pre> <p>That keeps extras flattened for packaging while preserving a maintainable source-of-truth dependency graph.</p>"},{"location":"#useful-areas","title":"Useful Areas","text":"<ul> <li><code>corio.path</code>: typed path helpers, app paths, file type guessing, serialization helpers.</li> <li><code>corio.dm</code> and <code>corio.sets</code>: pydantic-oriented models and settings/CLI support.</li> <li><code>corio.api</code>: FastAPI and MCP endpoint composition.</li> <li><code>corio.iterator</code>: progress-aware iterable wrapper and small collection helpers.</li> <li><code>corio.infra</code>: release, docs, tox, and pyproject generation tooling.</li> <li><code>corio.db.search</code>: vector search helpers around qdrant and embeddings.</li> <li><code>corio.tabular</code>, <code>corio.semantic</code>, <code>corio.metric</code>: data and retrieval metrics helpers.</li> <li><code>corio.encrypt</code>, <code>corio.sec</code>: project secret and encryption workflows.</li> <li><code>corio.ha</code>, <code>corio.dns</code>, <code>corio.mqtt</code>, <code>corio.webhook</code>: operations and home automation integrations.</li> </ul>"},{"location":"#documentation","title":"Documentation","text":"<ul> <li>Published docs: https://fmtr.github.io/corio</li> <li>Local docs source: docs</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is licensed under Apache 2.0. See LICENSE.</p>"},{"location":"quick-start/","title":"Quick Start","text":""},{"location":"quick-start/#install-base-package","title":"Install Base Package","text":"<pre><code>pip install corio\n</code></pre>"},{"location":"quick-start/#install-extras","title":"Install Extras","text":"<p><code>corio</code> keeps optional dependencies behind extras:</p> <pre><code>pip install \"corio[dm,http,api]\" --upgrade\n</code></pre> <p>You can mix as many extras as needed for your project.</p>"},{"location":"quick-start/#basic-usage","title":"Basic Usage","text":"<pre><code>import corio\nfrom corio import Path\n\nport = corio.env.get_int(\"APP_PORT\", default=8080)\nPath(\"runtime.json\").write_json({\"port\": port})\n</code></pre>"},{"location":"quick-start/#missing-extra-errors","title":"Missing Extra Errors","text":"<p>If a module needs an extra that is not installed, the import surfaces a clear guidance message with the required extra name.</p> <p>Use the Modules Overview table to see import names and extras.</p>"},{"location":"changelog/2.2.2/","title":"2.2.2","text":"<ul> <li>Add unit testing to release flow.</li> <li>Add editable dependency handling with version pinning.</li> <li>Allow editable dependency pinning with extras.</li> <li>Allow pre-release projects to pin pre-release editable dependencies.</li> <li>Simplify infra and expand unit test coverage for editable dependency pinning.</li> <li>Fix test execution order (tests now run after version increment logic is applied correctly).</li> </ul>"},{"location":"changelog/2.6.1/","title":"2.6.1","text":"<ul> <li>Expanded <code>db.search</code> with multi-vector queries, an evaluator, and a fuller example module.</li> <li>Added clearer documentation for the search API so it is easier to understand and use.</li> <li>Refactored the agentic helpers into a subpackage and introduced shared tool and toolset base classes.</li> <li>Standardized debug arguments to snake_case and updated the default host.</li> <li>Updated <code>trim</code> so it can work with multiple strings.</li> <li>Fixed a broken <code>flagembedding</code> import.</li> </ul>"},{"location":"modules/","title":"Modules Overview","text":"<p>This table maps friendly imports from <code>corio.__init__</code> to docs pages and required extras.</p> <p><code>Extra(s)</code> refers to <code>tool.corio.dependencies</code> in <code>pyproject.toml</code>.</p> Friendly import Extra(s) Reference <code>from corio import ai</code> <code>ai</code> ai <code>from corio import aio</code> - aio <code>from corio import api</code> <code>api</code> api <code>from corio import augmentation</code> <code>augmentation</code> augmentation <code>from corio import av</code> <code>av</code> av <code>from corio import caching</code> <code>caching</code> caching <code>from corio import dataclass</code> - dataclass <code>from corio import datatype</code> - datatype <code>from corio import db</code> <code>db</code> / <code>db.document</code> db <code>from corio import debug</code> <code>debug</code> debug <code>from corio import dm</code> <code>dm</code> dm <code>from corio import dns</code> <code>dns</code> dns <code>from corio import docker</code> <code>docker.client</code> docker <code>from corio import dt</code> - dt <code>from corio import encrypt</code> <code>encrypt</code> encrypt <code>from corio import env</code> - (<code>env.io</code> for dotenv helpers) env <code>from corio import function</code> - function <code>from corio import google_api</code> <code>google.api</code> google_api <code>from corio import ha</code> <code>ha</code> (<code>ha.api</code> for API/supervisor extras) ha <code>from corio import hash</code> - hash <code>from corio import hfh</code> <code>hfh</code> hfh <code>from corio import markup</code> <code>html</code> markup <code>from corio import https</code> <code>http</code> https <code>from corio import import_</code> - import <code>from corio import infra</code> <code>infra</code> infra <code>from corio import inherit</code> - inherit <code>from corio import interface</code> <code>interface</code> interface <code>from corio import iterator</code> - iterator <code>from corio import jsn</code> - jsn <code>from corio import json_fix</code> <code>json-fix</code> json_fix <code>from corio import logs</code> <code>logging</code> logs <code>from corio import merging</code> <code>merging</code> merging <code>from corio import metric</code> <code>metric</code> metric <code>from corio import mqtt</code> <code>mqtt</code> mqtt <code>from corio import name</code> - name <code>from corio import net</code> - net <code>from corio import netrc</code> <code>netrc</code> netrc <code>from corio import openai</code> <code>openai.api</code> openai <code>from corio import packaging</code> - packaging <code>from corio import parallel</code> <code>parallel</code> parallel <code>from corio import path</code> - (<code>path.app</code>, <code>path.type</code> for extras) path <code>from corio import patterns</code> <code>patterns</code> patterns <code>from corio import pdf</code> <code>pdf</code> pdf <code>from corio import plat</code> - plat <code>from corio import process</code> <code>process</code> process <code>from corio import profiling</code> <code>profiling</code> profiling <code>from corio import rand</code> - rand <code>from corio import sec</code> <code>secrets</code> secrets <code>from corio import semantic</code> <code>semantic</code> semantic <code>from corio import sets</code> <code>sets</code> sets <code>from corio import setup</code> <code>setup</code> setup <code>from corio import spaces</code> <code>spaces</code> spaces <code>from corio import strings</code> - strings <code>from corio import tabular</code> <code>tabular</code> tabular <code>from corio import toml</code> - toml <code>from corio import tokenization</code> <code>tokenization</code> tokenization <code>from corio import unicode</code> <code>unicode</code> unicode <code>from corio import vcs</code> <code>vcs</code> vcs <code>from corio import version</code> <code>version</code> / <code>version.dev</code> version <code>from corio import webhook</code> <code>webhook</code> webhook <code>from corio import yml</code> <code>yaml</code> yml <code>from corio import youtube</code> <code>youtube</code> youtube"},{"location":"modules/#shared-top-level-exports","title":"Shared Top-Level Exports","text":"<ul> <li><code>from corio import Path, PackagePaths, AppPaths</code> -&gt; path</li> <li><code>from corio import logger</code> -&gt; logs</li> <li><code>from corio import Constants</code> -&gt; path</li> <li><code>from corio import Client</code> -&gt; https</li> <li><code>from corio import Timer</code> -&gt; profiling</li> <li><code>from corio import ContextProcess</code> -&gt; process</li> <li><code>from corio import merge</code> -&gt; merging</li> </ul>"},{"location":"modules/ai/","title":"ai","text":"<p><code>from corio import ai</code></p> <p>Tools for model inference and agentic helpers.</p> <p>Install: <code>pip install \"corio[ai]\" --upgrade</code></p>"},{"location":"modules/aio/","title":"aio","text":"<p><code>from corio import aio</code></p> <p>Async utility helpers.</p>"},{"location":"modules/api/","title":"api","text":"<p><code>from corio import api</code></p> <p><code>api</code> provides a lightweight FastAPI base where endpoints are defined as classes.</p> <p>Main pieces:</p> <ul> <li><code>endpoint.Base</code>: base endpoint contract (<code>run</code>)</li> <li><code>endpoint.API</code>: class-based HTTP endpoint with registration helpers</li> <li><code>endpoint.MCP</code>: class-based MCP endpoint with tool registration helpers</li> <li><code>endpoint.Tool</code>: MCP tool endpoint</li> <li><code>endpoint.Prompt</code>: MCP prompt endpoint</li> <li><code>endpoint.Resource</code>: MCP resource endpoint</li> <li><code>Base</code>: app bootstrap, endpoint registration, child API mounting, and uvicorn launch helpers</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[api]\" --upgrade\n</code></pre>"},{"location":"modules/api/#typical-endpoint-pattern","title":"Typical Endpoint Pattern","text":"<pre><code>from __future__ import annotations\n\nfrom corio import api\n\n\nclass HealthApi(api.Base):\n    TITLE = \"Health API\"\n\n    @property\n    def ENDPOINTS(self):\n        return [Health]\n\n    @property\n    def TRANSFORMS(self):\n        return []\n\n\nclass Health(api.endpoint.API):\n    \"\"\"Service health.\"\"\"\n\n    PATH = \"/health\"\n    TAGS = \"status\"\n\n    async def run(self):\n        return {\"ok\": True}\n</code></pre> <p>Run with:</p> <pre><code>HealthApi.launch()\n</code></pre>"},{"location":"modules/api/#real-world-usage-pattern-generalized","title":"Real-World Usage Pattern (generalized)","text":"<p>Sibling repos commonly:</p> <ul> <li>subclass <code>api.Base</code></li> <li>keep endpoint class list in <code>ENDPOINTS</code> (can include <code>api.endpoint.API</code> and <code>api.endpoint.MCP</code>)</li> <li>keep MCP transform class list in <code>TRANSFORMS</code> (each class is initialized with <code>self.mcp</code>)</li> <li>attach tags per feature area (<code>cache</code>, <code>blocking</code>, <code>documents</code>, etc.)</li> <li>mount child APIs using <code>children</code> when composing a larger API surface</li> </ul>"},{"location":"modules/api/#notes","title":"Notes","text":"<ul> <li><code>Base</code> instruments FastAPI via <code>corio.logs</code>.</li> <li>In dev mode, exception handling is configured to re-raise for easier debugging.</li> <li>Endpoints register as GET routes.</li> <li>MCP endpoint classes register tools via <code>self.mcp.tool(...)</code>.</li> <li>Default endpoint path is <code>/{endpoint_class_name_lower}</code> unless <code>PATH</code> is overridden.</li> </ul>"},{"location":"modules/augmentation/","title":"augmentation","text":"<p><code>from corio import augmentation</code></p> <p>Data augmentation helpers (currently lightweight/stub-level).</p> <p>Install: <code>pip install \"corio[augmentation]\" --upgrade</code></p>"},{"location":"modules/av/","title":"av","text":"<p><code>from corio import av</code></p> <p>Audio/video helpers.</p> <p>Install: <code>pip install \"corio[av]\" --upgrade</code></p>"},{"location":"modules/caching/","title":"caching","text":"<p><code>from corio import caching</code></p> <p>Caching helpers and wrappers.</p> <p>Install: <code>pip install \"corio[caching]\" --upgrade</code></p>"},{"location":"modules/dataclass/","title":"dataclass","text":"<p><code>from corio import dataclass</code></p> <p>Dataclass utilities for field filtering/introspection.</p>"},{"location":"modules/datatype/","title":"datatype","text":"<p><code>from corio import datatype</code></p> <p>General datatype inspection and conversion helpers.</p>"},{"location":"modules/db/","title":"db","text":"<p><code>from corio import db</code></p> <p>Database helper package namespace.</p> <ul> <li>Base namespace uses <code>db</code>.</li> <li>Document-model integrations use <code>db.document</code>.</li> </ul>"},{"location":"modules/debug/","title":"debug","text":"<p><code>from corio import debug</code></p> <p>Debugging helpers and tooling wrappers.</p> <p>Install: <code>pip install \"corio[debug]\" --upgrade</code></p>"},{"location":"modules/dm/","title":"dm","text":"<p><code>from corio import dm</code></p> <p><code>dm</code> provides the data modelling layer built on Pydantic, with utilities aimed at robust parsing and schema ergonomics.</p> <p>Highlights:</p> <ul> <li><code>Field</code>: reusable field definitions with optional auto-defaulting and title/description templating</li> <li><code>Base</code>: model base that can aggregate <code>FIELDS</code> declarations across inheritance</li> <li><code>MixinFromJson</code>: tolerant <code>from_json</code> flow (especially useful for LLM output cleanup)</li> <li><code>to_df</code>: quick conversion to tabular representation (<code>tabular</code> extra needed there)</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[dm]\" --upgrade\n</code></pre>"},{"location":"modules/dm/#typical-model-pattern","title":"Typical Model Pattern","text":"<pre><code>from corio import dm\n\n\nclass User(dm.Base):\n    id: int\n    name: str\n\n\nuser = User.from_json('{\"id\": 1, \"name\": \"Ada\"}')\n</code></pre>"},{"location":"modules/dm/#field-class-pattern","title":"Field Class Pattern","text":"<p><code>dm.Field</code> is useful when you want reusable field components across multiple models:</p> <pre><code>from corio import dm\n\n\nclass FieldName(dm.Field):\n    ANNOTATION = str\n    DESCRIPTION = \"Human-readable user name.\"\n\n\nclass User(dm.Base):\n    FIELDS = [FieldName]\n    id: int\n</code></pre>"},{"location":"modules/dm/#llmunclean-json-use","title":"LLM/Unclean JSON Use","text":"<p><code>MixinFromJson</code> routes through <code>json_fix</code> when available, so model parsing can be more tolerant than strict <code>model_validate_json</code>.</p> <p>This is especially useful for:</p> <ul> <li>tool-call payload recovery</li> <li>partially malformed JSON responses</li> <li>low-friction CLI/data ingestion flows</li> </ul>"},{"location":"modules/dm/#cli-integration","title":"CLI Integration","text":"<p><code>CliRunMixin</code> enables subcommand-style <code>run()</code> flow for settings/models that integrate with <code>pydantic-settings</code> subcommands.</p>"},{"location":"modules/dns/","title":"dns","text":"<p><code>from corio import dns</code></p> <p><code>dns</code> contains a small DNS toolkit for building resolvers/proxies, with request/response datamodels and plain or DoH upstream clients.</p> <p>Main parts:</p> <ul> <li><code>dns.dm</code>: exchange/request/response models (<code>Exchange</code>, <code>Request</code>, <code>Response</code>)</li> <li><code>dns.client</code>: upstream clients (<code>Plain</code>, <code>HTTP</code>)</li> <li><code>dns.server</code>: async UDP server base with caching and structured handling</li> <li><code>dns.proxy</code>: proxy server base that lets you hook <code>process_question</code> and <code>process_upstream</code></li> </ul> <p>Install:</p> <pre><code>pip install \"corio[dns]\" --upgrade\n</code></pre>"},{"location":"modules/dns/#request-flow","title":"Request Flow","text":"<p>The base <code>dns.proxy.Proxy</code> model is stage-based:</p> <ol> <li><code>process_question(exchange)</code> - mutate/short-circuit before upstream.</li> <li>upstream resolve (<code>dns.client.Plain</code> or <code>dns.client.HTTP</code>).</li> <li><code>process_upstream(exchange)</code> - inspect/replace upstream answers.</li> <li><code>finalize(exchange)</code> - finalize message and completion state.</li> </ol> <p><code>dns.server.Plain</code> handles UDP datagrams, cache check/write, reverse lookup context, and response send.</p> <pre><code>from corio import dns\n\n\nclass PolicyProxy(dns.proxy.Proxy):\n    def process_question(self, exchange):\n        # rewrite or block before upstream\n        return\n\n    def process_upstream(self, exchange):\n        # inspect upstream answers, optionally block\n        return\n</code></pre>"},{"location":"modules/dns/#upstream-selection","title":"Upstream Selection","text":"<p><code>dns.client.HTTP</code> is used for DoH, and can be selected dynamically by wrapping clients in a rule-driven selector (for example via <code>patterns.Transformer</code> subclasses).</p>"},{"location":"modules/dns/#data-model-objects","title":"Data Model Objects","text":"<ul> <li><code>dns.dm.Request</code>: parsed question and helper metadata.</li> <li><code>dns.dm.Response</code>: response/message wrapper with <code>rcode</code>, <code>ttl</code>, answer access.</li> <li><code>dns.dm.Exchange</code>: mutable exchange state from ingress to final response.</li> </ul>"},{"location":"modules/docker/","title":"docker","text":"<p><code>from corio import docker</code></p> <p>Docker client integrations used by infrastructure modules.</p> <p>Install: <code>pip install \"corio[docker.client]\" --upgrade</code></p>"},{"location":"modules/dt/","title":"dt","text":"<p><code>from corio import dt</code></p> <p>Datetime helper utilities.</p>"},{"location":"modules/encrypt/","title":"encrypt","text":"<p><code>from corio import encrypt</code></p> <p><code>encrypt</code> provides AGE-based encryption helpers for serializable Python data.</p> <p>Classes:</p> <ul> <li><code>Encryptor</code>: encrypt/decrypt full payloads</li> <li><code>EncryptorValues</code>: tree transform that encrypts/decrypts leaf values</li> <li><code>EncryptorValuesSelect</code>: selective node encryption using path-match patterns</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[encrypt]\" --upgrade\n</code></pre>"},{"location":"modules/encrypt/#behavior","title":"Behavior","text":"<ul> <li>Values are serialized to JSON before encryption.</li> <li>Cipher payloads are wrapped in a recognizable header marker.</li> <li><code>EncryptorValues*</code> preserves tree structure and only transforms leaves.</li> </ul>"},{"location":"modules/encrypt/#example-selective-node-encryption","title":"Example: Selective Node Encryption","text":"<pre><code>from corio import encrypt\n\ndata = {\n    \"service\": {\"token\": \"abc\", \"region\": \"eu\"},\n    \"db\": {\"password\": \"secret\", \"port\": 5432},\n}\n\nenc = encrypt.EncryptorValuesSelect(nodes=[\"service/token\", \"db/password\"])\ncipher = enc.encrypt(data)\nplain = enc.decrypt(cipher)\n</code></pre> <p><code>nodes</code> accepts path-style match expressions evaluated against leaf nodes.</p> <p>By default the key is read from the <code>FMTR_AGE_KEY</code> environment variable.</p>"},{"location":"modules/env/","title":"env","text":"<p><code>from corio import env</code></p> <p>Environment variable getters and type-safe parsers.</p> <p>Dotenv helpers are available through the <code>env.io</code> extra where needed.</p>"},{"location":"modules/function/","title":"function","text":"<p><code>from corio import function</code></p> <p>Function signature and argument utility helpers.</p>"},{"location":"modules/google_api/","title":"google_api","text":"<p><code>from corio import google_api</code></p> <p>Google API authentication/client helpers.</p> <p>Install: <code>pip install \"corio[google.api]\" --upgrade</code></p>"},{"location":"modules/ha/","title":"ha","text":"<p><code>from corio import ha</code></p> <p>Home Assistant related helpers.</p> <ul> <li>Core helper set: <code>ha</code></li> <li>API/supervisor integration extras: <code>ha.api</code></li> </ul>"},{"location":"modules/hash/","title":"hash","text":"<p><code>from corio import hash</code></p> <p>Hashing helpers for common string/object use cases.</p>"},{"location":"modules/hfh/","title":"hfh","text":"<p><code>from corio import hfh</code></p> <p>Hugging Face Hub/cache helpers.</p> <p>Install: <code>pip install \"corio[hfh]\" --upgrade</code></p>"},{"location":"modules/https/","title":"https","text":"<p><code>from corio import https</code></p> <p>Instrumented HTTP client helpers with retries.</p> <p>Install: <code>pip install \"corio[http]\" --upgrade</code></p> <p>Also exported at top level: <code>from corio import Client</code>.</p>"},{"location":"modules/import/","title":"import","text":"<p><code>from corio import import_</code></p> <p>Import-time helpers, including missing-extra handling wrappers.</p>"},{"location":"modules/infra/","title":"infra","text":"<p><code>from corio import infra</code></p> <p><code>infra</code> is the release/build orchestration layer used by <code>corio</code> projects.</p> <p>Core objects:</p> <ul> <li><code>Project</code>: package metadata + runtime release context</li> <li><code>Repository</code>: git operations (<code>fetch</code>, tagging, push flow)</li> <li><code>Stack</code>: container build/deploy definitions (development and production variants)</li> <li><code>Releaser</code>: version increment, package build/upload, docs deploy flow</li> <li><code>infrastructure.api.Api</code>: FastAPI surface for release/build/recreate operations</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[infra]\" --upgrade\n</code></pre>"},{"location":"modules/infra/#typical-usage","title":"Typical Usage","text":"<pre><code>from corio import infra\n\nproject = infra.Project(\"corio\")\nproject.releaser.run(build=False, release=True, cache=True)\n</code></pre>"},{"location":"modules/infra/#api-usage","title":"API Usage","text":"<p><code>infra.Api</code> exposes release/build/recreate endpoints and is used by infra wrapper repos:</p> <pre><code>from corio import infra\n\ninfra.Api.launch()\n</code></pre>"},{"location":"modules/infra/#docs-release-expectations","title":"Docs Release Expectations","text":"<p>Docs release expects:</p> <ul> <li><code>docs/</code> content in repo root</li> <li><code>tool.corio.metadata.docs.nav</code> in <code>pyproject.toml</code></li> </ul> <p><code>ReleaseDocumentation</code> builds MkDocs configuration dynamically from project metadata, so nav structure in <code>pyproject</code> is part of the release contract.</p>"},{"location":"modules/infra/#release-flow-high-level","title":"Release Flow (high-level)","text":"<p><code>releaser.run(...)</code> orchestrates:</p> <ol> <li>version increment + git/tag flow</li> <li>optional container build/push</li> <li>package build/upload</li> <li>docs deploy</li> </ol>"},{"location":"modules/inherit/","title":"inherit","text":"<p><code>from corio import inherit</code></p> <p>Simple inheritance/composition helper utilities.</p>"},{"location":"modules/interface/","title":"interface","text":"<p><code>from corio import interface</code></p> <p><code>interface</code> is a thin base for Flet UI apps with lifecycle hooks and typed context wiring.</p> <p>Main pieces:</p> <ul> <li><code>Context</code>: typed runtime container around <code>flet.Page</code></li> <li><code>Base</code>: <code>ft.Column</code> subclass with async constructor (<code>new</code>), routing hooks, and launch helper</li> <li>decorators <code>update</code> and <code>progress</code> for post-action page refresh/progress display</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[interface]\" --upgrade\n</code></pre>"},{"location":"modules/interface/#typical-base-pattern","title":"Typical Base Pattern","text":"<pre><code>import flet as ft\nfrom corio import interface\n\n\nclass App(interface.Base[interface.Context]):\n    TITLE = \"Example Interface\"\n\n    def __init__(self):\n        super().__init__(controls=[ft.Text(self.TITLE)])\n</code></pre> <pre><code>App.launch()\n</code></pre>"},{"location":"modules/interface/#advanced-pattern-seen-in-sibling-repos","title":"Advanced Pattern (seen in sibling repos)","text":"<ul> <li>Override <code>async def new(cls, page)</code> for async setup (database/session/bootstrap).</li> <li>Configure <code>PATH_ASSETS</code> and <code>PATH_UPLOADS</code> for static/upload handling.</li> <li>Use tabs/views and route hooks (<code>route</code>, <code>pop</code>) for multi-view apps.</li> <li><code>Base</code> auto-manages <code>FLET_SECRET_KEY</code> when missing.</li> </ul> <pre><code>@classmethod\nasync def new(cls, page):\n    # async startup work here\n    self = await super().new(page)\n    return self\n</code></pre> <p><code>update</code> and <code>progress</code> decorators are useful for callback methods that need guaranteed page refresh and spinner visibility.</p>"},{"location":"modules/iterator/","title":"iterator","text":"<p><code>from corio import iterator</code></p> <p>Iterator and collection helpers (<code>ilist</code>, <code>fdict</code>, flattening, enlist, etc).</p>"},{"location":"modules/jsn/","title":"jsn","text":"<p><code>from corio import jsn</code></p> <p>JSON serializer/deserializer helpers with human-readable defaults.</p>"},{"location":"modules/json_fix/","title":"json_fix","text":"<p><code>from corio import json_fix</code></p> <p>Tolerant JSON repair/parsing helpers.</p> <p>Install: <code>pip install \"corio[json-fix]\" --upgrade</code></p>"},{"location":"modules/logs/","title":"logs","text":"<p><code>from corio import logs</code></p> <p>Logging setup and instrumentation helpers.</p> <p>Install: <code>pip install \"corio[logging]\" --upgrade</code></p> <p>Also exported at top level: <code>from corio import logger</code>.</p>"},{"location":"modules/markup/","title":"markup","text":"<p><code>from corio import markup</code></p> <p>HTML to text conversion helpers.</p> <p>Install: <code>pip install \"corio[html]\" --upgrade</code></p>"},{"location":"modules/merging/","title":"merging","text":"<p><code>from corio import merging</code></p> <p>Recursive merge helpers.</p> <p>Install: <code>pip install \"corio[merging]\" --upgrade</code></p> <p>Also exported at top level: <code>from corio import merge</code>.</p>"},{"location":"modules/metric/","title":"metric","text":"<p><code>from corio import metric</code></p> <p>Metric/evaluation helpers, mostly tabular-data oriented.</p> <p>Install: <code>pip install \"corio[metric]\" --upgrade</code></p>"},{"location":"modules/mqtt/","title":"mqtt","text":"<p><code>from corio import mqtt</code></p> <p>MQTT utility helpers.</p> <p>Install: <code>pip install \"corio[mqtt]\" --upgrade</code></p>"},{"location":"modules/name/","title":"name","text":"<p><code>from corio import name</code></p> <p>Random memorable name generators.</p>"},{"location":"modules/net/","title":"net","text":"<p><code>from corio import net</code></p> <p>Networking helpers.</p>"},{"location":"modules/netrc/","title":"netrc","text":"<p><code>from corio import netrc</code></p> <p>Netrc helper wrappers.</p> <p>Install: <code>pip install \"corio[netrc]\" --upgrade</code></p>"},{"location":"modules/openai/","title":"openai","text":"<p><code>from corio import openai</code></p> <p>OpenAI integration helpers.</p> <p>Install: <code>pip install \"corio[openai.api]\" --upgrade</code></p>"},{"location":"modules/packaging/","title":"packaging","text":"<p><code>from corio import packaging</code></p> <p>Packaging utility helpers.</p>"},{"location":"modules/parallel/","title":"parallel","text":"<p><code>from corio import parallel</code></p> <p>Parallel execution helpers.</p> <p>Install: <code>pip install \"corio[parallel]\" --upgrade</code></p>"},{"location":"modules/path/","title":"path","text":"<p><code>from corio import path</code></p> <p><code>path</code> is one of the core modules in <code>corio</code>. It extends <code>pathlib.Path</code> and provides package-aware path resolution helpers used throughout the rest of the library.</p> <p>Primary exports:</p> <ul> <li><code>Path</code>: enhanced path object with convenience readers/writers (<code>json</code>, <code>yaml</code>, <code>toml</code>, <code>.env</code>, inferred <code>read_data</code>/<code>write_data</code>)</li> <li><code>PackagePaths</code>: canonical package/repo metadata and path discovery</li> <li><code>AppPaths</code>: appdirs wrapper (<code>path.app</code> extra)</li> </ul>"},{"location":"modules/path/#what-is-useful-here","title":"What Is Useful Here","text":"<ul> <li><code>Path.read_data()</code> / <code>Path.write_data(obj)</code> infer serializer by suffix:</li> <li><code>.json</code> -&gt; JSON</li> <li><code>.yaml</code> / <code>.yml</code> -&gt; YAML</li> <li><code>.toml</code> -&gt; TOML</li> <li><code>.env</code> -&gt; dotenv map</li> <li>unknown -&gt; text</li> <li><code>Path.find_up(name)</code> walks up parent directories for repo-root style config lookup.</li> <li><code>Path.mkdirf()</code> is a <code>parents=True, exist_ok=True</code> convenience.</li> <li><code>PackagePaths().metadata</code> gives strongly-typed access to <code>tool.corio.metadata</code> from package <code>pyproject</code>.</li> <li>WSL absolute Windows paths are auto-converted when running under WSL.</li> </ul>"},{"location":"modules/path/#examples","title":"Examples","text":"<pre><code>from corio import Path\n\npath = Path(\"settings.yaml\")\nconfig = path.read_data()  # same as read_yaml() here\n\nout = Path(\"build/runtime.toml\")\nout.parent.mkdirf()\nout.write_data({\"name\": \"corio\", \"enabled\": True})\nruntime = out.read_data()\n</code></pre> <pre><code>from corio import PackagePaths\n\npaths = PackagePaths()\nprint(paths.repo)\nprint(paths.metadata.version)\nprint(paths.metadata.description)\n</code></pre> <pre><code>from corio import Path\n\npath_secrets = Path.cwd().find_up(\".secrets.yml\")\nprint(path_secrets)\n</code></pre>"},{"location":"modules/path/#serializer-methods","title":"Serializer Methods","text":"<p><code>Path</code> also exposes explicit methods when you do not want extension inference:</p> <ul> <li><code>read_json()</code> / <code>write_json()</code></li> <li><code>read_yaml()</code> / <code>write_yaml()</code></li> <li><code>read_toml()</code> / <code>write_toml()</code></li> <li><code>read_env()</code> / <code>write_env()</code></li> </ul>"},{"location":"modules/patterns/","title":"patterns","text":"<p><code>from corio import patterns</code></p> <p><code>patterns</code> is a regex-driven transformation utility for structured rewrite rules.</p> <p>Main types:</p> <ul> <li><code>Key</code>: serializable pattern key with named capture support</li> <li><code>Item</code>: source/target rule pair</li> <li><code>Transformer</code>: compiles many rules into one expression and applies one-pass or recursive rewrite</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[patterns]\" --upgrade\n</code></pre>"},{"location":"modules/patterns/#why-use-it","title":"Why Use It","text":"<p>Use this when dictionary-style lookups are not enough and you need:</p> <ul> <li>regex capture groups</li> <li>templated target rewrites</li> <li>recursive transforms with loop detection</li> </ul>"},{"location":"modules/patterns/#real-world-pattern-generalized","title":"Real-world Pattern (generalized)","text":"<p>In DNS tooling, repos use:</p> <ul> <li><code>Key</code> subclasses (<code>name</code>, <code>records</code>)</li> <li><code>Item</code> rules mapping a source key to transformed key or sentinel value</li> <li><code>Transformer.get(key)</code> to select upstream behavior or blocklist output</li> </ul>"},{"location":"modules/patterns/#example","title":"Example","text":"<pre><code>from dataclasses import dataclass\nfrom corio import patterns\n\n\n@dataclass\nclass HostKey(patterns.Key):\n    FILLS = {\"sub\": r\"[a-z0-9-]+\"}\n    host: str\n\n\nrules = [\n    patterns.Item(source=HostKey(host=r\"{sub}\\.example\\.com\"), target=HostKey(host=r\"edge-{sub}.example.net\")),\n]\n\ntx = patterns.Transformer(items=rules, default=None)\n</code></pre> <p>If recursive mode is enabled and rules loop, <code>RewriteCircularLoopError</code> is raised.</p>"},{"location":"modules/pdf/","title":"pdf","text":"<p><code>from corio import pdf</code></p> <p>PDF extraction and processing helpers.</p> <p>Install: <code>pip install \"corio[pdf]\" --upgrade</code></p>"},{"location":"modules/plat/","title":"plat","text":"<p><code>from corio import plat</code></p> <p>Platform/environment detection helpers.</p>"},{"location":"modules/process/","title":"process","text":"<p><code>from corio import process</code></p> <p>Process lifecycle helpers.</p> <p>Install: <code>pip install \"corio[process]\" --upgrade</code></p> <p>Also exported at top level: <code>from corio import ContextProcess</code>.</p>"},{"location":"modules/profiling/","title":"profiling","text":"<p><code>from corio import profiling</code></p> <p>Timing and profiling helpers.</p> <p>Install: <code>pip install \"corio[profiling]\" --upgrade</code></p> <p>Also exported at top level: <code>from corio import Timer</code>.</p>"},{"location":"modules/rand/","title":"rand","text":"<p><code>from corio import rand</code></p> <p>Random helper functions.</p>"},{"location":"modules/secrets/","title":"secrets","text":"<p><code>from corio import sec</code></p> <p><code>secrets</code> builds on <code>encrypt</code> + <code>sets</code> to provide repo-oriented secret file workflows.</p> <p>Main model:</p> <ul> <li><code>Config</code>: reads <code>.secrets.yml</code>, definitions, and contexts</li> <li><code>Encrypt</code> command: writes encrypted <code>&lt;raw&gt;.black.yml</code> files from <code>&lt;raw&gt;.red.yml</code> files</li> <li><code>Decrypt</code> command: writes <code>&lt;raw&gt;.red.yml</code> files back from <code>&lt;raw&gt;.black.yml</code> when allowed</li> </ul> <p>Install:</p> <pre><code>pip install \"corio[secrets]\" --upgrade\n</code></pre>"},{"location":"modules/secrets/#high-level-behavior","title":"High-level behavior","text":"<ol> <li>Define raw file globs and encrypted nodes in <code>.secrets.yml</code>; the <code>.red.yml</code> suffix is implicit.</li> <li>Run encrypt mode to produce <code>*.black.yml</code>.</li> <li>Run decrypt mode for selected contexts when needed.</li> </ol> <p>Any <code>*.red.yml</code> file not covered by a definition is still encrypted. All of its fields are encrypted and a warning is logged.</p> <p>Encryption deletes each red file by default after its black counterpart has been written or verified. Pass <code>--delete false</code> to retain red files. Decryption writes red files by default; pass <code>--restore true</code> to write each value to its original filename and format instead. Pass <code>--force true</code> to overwrite every target regardless of its age or current contents, and <code>--preserve true</code> to copy the black file's permissions and ownership to the decrypted target.</p>"},{"location":"modules/secrets/#minimal-secretsyml-shape","title":"Minimal <code>.secrets.yml</code> shape","text":"<pre><code>contexts:\n  - name: web\n    files:\n      - \"services/web/**/*.yml\"\n\ndefinitions:\n  - files:\n      - \"services/web/**/*.yml\"\n    nodes:\n      - \"env/password\"\n      - \"env/token\"\n</code></pre>"},{"location":"modules/secrets/#cli","title":"CLI","text":"<p>Entry point:</p> <pre><code>corio secrets encrypt --context=web\ncorio secrets decrypt --context=web\n</code></pre> <p>For example, the definition path <code>services/web/config.yaml</code> matches <code>services/web/config.yaml.red.yml</code>. Encryption writes <code>services/web/config.yaml.black.yml</code>, and decryption restores the red file when the encrypted side is newer or changed.</p>"},{"location":"modules/semantic/","title":"semantic","text":"<p><code>from corio import semantic</code></p> <p>Semantic similarity/vector search helpers.</p> <p>Install: <code>pip install \"corio[semantic]\" --upgrade</code></p>"},{"location":"modules/sets/","title":"sets","text":"<p><code>from corio import sets</code></p> <p>Settings management built on <code>pydantic-settings</code>.</p> <p>Install: <code>pip install \"corio[sets]\" --upgrade</code></p>"},{"location":"modules/setup/","title":"setup","text":"<p><code>from corio import setup</code></p> <p>Setup/build helper wrappers.</p> <p>Install: <code>pip install \"corio[setup]\" --upgrade</code></p>"},{"location":"modules/spaces/","title":"spaces","text":"<p><code>from corio import spaces</code></p> <p>Spaces-related helpers (netrc-based integrations).</p> <p>Install: <code>pip install \"corio[spaces]\" --upgrade</code></p>"},{"location":"modules/strings/","title":"strings","text":"<p><code>from corio import strings</code></p> <p>String formatting and transform helpers.</p>"},{"location":"modules/tabular/","title":"tabular","text":"<p><code>from corio import tabular</code></p> <p>Tabular data helpers (DataFrame formatting/io).</p> <p>Install: <code>pip install \"corio[tabular]\" --upgrade</code></p>"},{"location":"modules/tokenization/","title":"tokenization","text":"<p><code>from corio import tokenization</code></p> <p>Tokenizer training/configuration helpers.</p> <p>Install: <code>pip install \"corio[tokenization]\" --upgrade</code></p>"},{"location":"modules/toml/","title":"toml","text":"<p><code>from corio import toml</code></p> <p>TOML parsing and serialization helpers.</p>"},{"location":"modules/unicode/","title":"unicode","text":"<p><code>from corio import unicode</code></p> <p>Unicode normalization/decoding helpers.</p> <p>Install: <code>pip install \"corio[unicode]\" --upgrade</code></p>"},{"location":"modules/vcs/","title":"vcs","text":"<p><code>from corio import vcs</code></p> <p>Version-control helpers exposed via <code>pygit2</code>.</p> <p>Install: <code>pip install \"corio[vcs]\" --upgrade</code></p> <p><code>vcs</code> is an external module import exposed at <code>corio</code> top level.</p>"},{"location":"modules/version/","title":"version","text":"<p><code>from corio import version</code></p> <p>Version parsing/increment helpers.</p> <p>Install: <code>pip install \"corio[version]\" --upgrade</code> (and <code>version.dev</code> where needed)</p>"},{"location":"modules/webhook/","title":"webhook","text":"<p><code>from corio import webhook</code></p> <p>Webhook request helpers built on the HTTP stack.</p> <p>Install: <code>pip install \"corio[webhook]\" --upgrade</code></p>"},{"location":"modules/yml/","title":"yml","text":"<p><code>from corio import yml</code></p> <p>YAML/YAMLScript read/write helpers.</p> <p>Install: <code>pip install \"corio[yaml]\" --upgrade</code></p>"},{"location":"modules/youtube/","title":"youtube","text":"<p><code>from corio import youtube</code></p> <p>YouTube download/metadata helpers.</p> <p>Install: <code>pip install \"corio[youtube]\" --upgrade</code></p>"}]}